function TenX(number) {
  const result = number * 10;
  return result;
}

function Half(number) {
  const result = number / 2;
  return result;
}

let numbers = TenX(100); // we can put function return value into a variable.
console.log(numbers);
