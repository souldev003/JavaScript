let fatherAge = 55;
let myAge = 23;

function difference(num1, num2) {
  let diff = num1 - num2;
  console.log(
    `First value is: ${num1} Second value is: ${num2}. Difference is: ${diff}`,
  );
}

difference(fatherAge, myAge);
